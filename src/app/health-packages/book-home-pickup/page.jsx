import HealthPackagesContent from '../HealthPackagesContent'

// Same page as /health-packages — the extra segment opens the home sample
// pickup form, so the open form has its own shareable URL.
export default function BookHomePickup() {
  return <HealthPackagesContent />
}
